1교시
리눅스 기초 명령어 
ls
ls -a 
touch week01.txt
cd git 
cd .
echo "daelim"
echo "deealim >> d

깃 기초 명령어 
git init ( 안하면 fatal 오류) --> .git 생성
git status (새로운 파일 생성시 untracted , 기존 clean , modified)
git add 파일명 (git stage에 올림)
git add . ( . == all)
git reset (git status에 올라간 track을 untracked로 바꿈)
git commit -m"커밋 메시지" (git)
git config --global "email"
git config --global "이름"


2교시 
git log
git checkout hashcode(7자리~max)
git checkout -


